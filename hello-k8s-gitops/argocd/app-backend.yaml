apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/banjoSolomon/Argocd'
    targetRevision: HEAD
    path: 'hello-k8s-gitops/gitops/backend'
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
